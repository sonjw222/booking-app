-- ============================================================
-- 센터별 결제수단 지정
--
-- centers.pay_methods (text[]) 추가
--   → 오너가 허용할 결제수단만 선택 (null/빈 배열이면 전체 허용)
--   결제화면에서 이 목록만 노출
--
-- 파일 전체를 SQL Editor에 붙여넣고 Run 하세요. 여러 번 실행해도 안전.
-- ============================================================

alter table centers add column if not exists pay_methods text[];


-- ============================================================
-- 완료!
--   관리자 모드 → 센터 정보 → 결제수단 선택
-- ============================================================
