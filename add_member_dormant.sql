-- ============================================================
-- 휴면회원 - 기간권 차감 정지
--
-- 하는 일:
--   center_members.dormant_since (휴면 시작 시각)
--   → 휴면 처리 시 기록, 활성 복귀 시 그 기간만큼 기간권 만료일 연장
--
-- 파일 전체를 SQL Editor에 붙여넣고 Run 하세요. 여러 번 실행해도 안전.
-- ============================================================

alter table center_members add column if not exists dormant_since timestamptz;


-- ============================================================
-- 완료!
--   회원 상세 → 회원 상태 → 휴면 처리 시 기간권 시간 정지
--   다시 활성 처리하면 휴면 기간만큼 만료일이 미뤄져요
-- ============================================================
