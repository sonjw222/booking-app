-- cleanup_p2_22_shared_center_class_fixtures_draft_proposed.sql 롤백 안내
--
-- 이 스크립트는 순수 데이터 삭제(DELETE)이므로 스키마 변경(컬럼/함수/제약조건)과 달리
-- SQL로 되돌릴 방법이 없습니다 — 삭제된 행은 값 자체가 사라지므로 "이전 정의로 재정의"
-- 같은 롤백이 성립하지 않습니다.
--
-- 되돌려야 하는 상황이 생기면:
--   1) 원본 cleanup SQL은 BEGIN/COMMIT 트랜잭션 안에서 실행됩니다 — 아직 COMMIT되지
--      않았다면(B 섹션 실행 중 이상함을 발견해 COMMIT 직전에 중단한 경우) ROLLBACK으로
--      되돌릴 수 있습니다. DO 블록의 RAISE EXCEPTION(3000건 초과 안전장치)이 발생한
--      경우는 이미 PostgreSQL이 전체 트랜잭션을 자동으로 롤백했으므로 별도 조치가
--      필요 없습니다.
--   2) 이미 COMMIT된 뒤라면, 삭제된 행은 실제 회원/매출 데이터가 전혀 아니라 전부
--      getOrCreateOwnedTestCenter()로 재사용되는 자동화 테스트 전용 공유센터
--      (3937eb89-3803-43e9-9a29-e893f779df1a)에 여러 테스트 파일이 남긴 leftover
--      class(및 거기 딸린 reservations)입니다. 이 센터는 실제 회원 대상 서비스에
--      노출된 적이 없습니다.
--      즉 "복구"가 아니라 "다음에 그 테스트가 필요할 때 다시 만들어내는 것"이 정답입니다
--      — 각 테스트 파일이 필요할 때마다 스스로 새 class를 만들도록 이미 작성돼 있어
--      별도 조치 없이 다음 실행부터 정상 동작합니다.
--   3) 정리 기준 자체(status='open' and start_time > now() and created_at < now() -
--      interval '1 hour', 이 센터로 한정)가 잘못 걸려 "지금 막 어떤 세션이 쓰고 있는"
--      class까지 지워졌다고 의심되면, 그 세션 쪽에 알리고 해당 테스트를 재실행해
--      필요한 class를 다시 만들게 하는 것이 유일하고 정상적인 대응입니다(created_at
--      1시간 마진이 이 상황을 원천 방지하도록 설계돼 있어 실제로 발생할 가능성은
--      낮습니다).
--   4) Supabase 프로젝트에 Point-in-Time Recovery(PITR)가 활성화돼 있다면, COMMIT 이후라도
--      해당 시점 이전으로 데이터베이스 전체를 복원하는 것은 이론적으로 가능하지만, 이는 이
--      스크립트가 건드리지 않은 다른 모든 변경사항까지 함께 되돌리므로 이 정리 하나만
--      되돌리는 용도로는 적절하지 않습니다.
