-- ============================================================
-- 센터 상세 화면: 소개글 컬럼 추가
--
-- 하는 일:
--   centers 에 intro (소개글) 컬럼 추가
--   → 매니저가 "센터 정보"에서 입력, 회원이 센터 상세 화면에서 봄
--
-- centers 수정 정책/트리거는 기존 것 그대로 사용합니다.
--   (매니저는 자기 센터 수정 가능, status 변경만 운영자 전용)
--
-- 파일 전체를 SQL Editor에 붙여넣고 Run 하세요. 여러 번 실행해도 안전.
-- ============================================================

alter table centers add column if not exists intro text;


-- ============================================================
-- 확인
-- ============================================================
select column_name from information_schema.columns
where table_name = 'centers' and column_name = 'intro';


-- ============================================================
-- 완료!
--   → 매니저 대시보드 → "센터 정보" → 소개/주소/연락처 입력 → 저장
--   → 홈에서 센터 누르면 → 센터 상세 화면에 표시
-- ============================================================
